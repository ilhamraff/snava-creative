/**
 * Static footer data — DEPRECATED.
 *
 * Footer and Final CTA data are now fetched from Payload CMS via
 * `getFooterData()` and `getFinalCtaData()` in get-site-settings.ts.
 *
 * This file is kept temporarily for reference. It can be safely deleted
 * once all consumers have been migrated.
 */
